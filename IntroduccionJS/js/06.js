//objeto MATH

let resultado;

resultado = Math.PI; //enseña el numero PI 
resultado = Math.round(2.2); //para redondear 
resultado = Math.ceil(2.2); //para redondear hacia arriba
resultado = Math.floor(2.2); //para redondear hacia abajo
resultado = Math.sqrt(25); //para la raiz cuadrada
resultado = Math.abs(-43); //lo convierte en positivo
resultado = Math.min(3, 4, 5, 10, 34); //nos indica cual es el numero mas pequeño de la lista
resultado = Math.max(3, 4, 5, 10, 34); //nos indica cual es el numero mas grande de la lista
resultado = Math.random(); //genera numero aleatorio
resultado = Math.floor(Math.random () * 30); //genera numero aleatorio del 0 al 30

console.log(resultado);